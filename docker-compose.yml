services:
  friend_me_bot:
    build:
      dockerfile: "Dockerfile"
    ports:
      - "8000:5000"
    environment:
      - BOT_TOKEN
      - AMP_TOKEN=ef58e9e7cdcfabb095f61798d8529d30
      - bot_name=friend_me_bot
    volumes:
      - ./:/root/app:cached
      - db_volume:/root/app/db

volumes: 
    db_volume: